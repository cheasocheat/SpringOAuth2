security.user.query=select email, usr_name, usr_password, rec_status from tb_user where email=?
security.role.query=select u.email, r.role_desc from tb_user u inner join tb_user_role ur on(u.usr_id=ur.usr_id) inner join tb_role r on(ur.role_id=r.role_id) where u.email=?
